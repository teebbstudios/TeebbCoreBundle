{% set serviceId = app.request.get('_teebb_entity_type') %}

{#Todo：还需要添加权限判断 需要获取可用actions #}

{% if entity_type.hasRoute(serviceId, 'create') %}
    <a href="{{ entity_type.pathInfoGenerator.generate(entity_type.EntityTypeMetadata.bundle ~ '_create') }}"
       class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">
        <i class="fas fa-plus-circle fa-sm text-white-50 pr-2"></i>
        {{ 'create'|trans }}{{ trans_markup(label) }}
    </a>
{% endif %}

