{% extends '@TeebbCore/base_content.html.twig' %}
{% set bundle = entity_type.bundle %}

{% block head_label %}
    {{ action|trans }}{{ bundle|trans }}
{% endblock %}

{% block card_body %}
    {{ dump(subject) }}
    {% set fieldDatas = entity_type.allFieldsData(subject, type_alias) %}
    {{ dump(fieldDatas) }}
    {{ form(form) }}
{% endblock %}
