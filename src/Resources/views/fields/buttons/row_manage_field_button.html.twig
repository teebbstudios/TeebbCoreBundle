{#列表行字段按钮#}
{#Todo: 需要权限判断#}

{% set bundle = entity_type.EntityTypeMetadata.bundle %}
{% set typeAlias = app.request.get('typeAlias') %}

<a href="{{ entity_type.pathInfoGenerator.generate( bundle ~ '_update_field', {'typeAlias':typeAlias, 'fieldAlias': field.fieldAlias|replace({'_':'-'}) }) }}"
   class="btn btn-sm btn-primary shadow-sm">
    <i class="fas fa-pen fa-sm text-white-50 pr-2"></i>编辑
</a>

<a href="{{ entity_type.pathInfoGenerator.generate( bundle ~ '_delete_field', {'typeAlias':typeAlias, 'fieldAlias': field.fieldAlias|replace({'_':'-'}) }) }}"
   class="btn btn-sm btn-danger shadow-sm">
    <i class="fas fa-trash-alt fa-sm text-white-50 pr-2"></i>删除
</a>